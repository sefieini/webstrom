// using caret
// ex. 4.a: switch to goal with minimum key presses. (using command shift v)

const data = {};
const value = {innerKey: 'value'};
data.key = value;

/*goal:
 console.log(data.key.innerKey);
 */



// ex. 4.b: remove other key
console.log(unneededWrapper.unneededWrapper2.data.key.unneededWrapper3.innerKey);

/*goal:
 console.log(data.key.innerKey);
 */






